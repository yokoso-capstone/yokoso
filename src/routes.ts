enum RoutePath {
  Home = "/",
  Search = "/search",
  Listings = "/listings",
  Dashboard = "/dashboard",
}

export enum RoutePathDashboard {
  Listings = "listings",
  Tenants = "tenants",
  Chat = "chat",
  Create = "create",
}

export default RoutePath;
