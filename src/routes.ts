enum RoutePath {
  Home = "/",
  Search = "/search",
  Listings = "/listings",
  Dashboard = "/dashboard",
}

export enum RoutePathDashboard {
  Listings = "listings",
  Tenants = "tenants",
  Chat = "chat",
  Create = "create",
  Requests = "requests",
}

export default RoutePath;
