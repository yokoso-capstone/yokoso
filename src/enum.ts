export enum DashboardLabel {
  Listings = "Listings",
  Tenants = "Tenants",
  Chat = "Chat",
  Create = "Create Listing",
  Requests = "Requests"
}

export enum ListingStatus {
  Available = "available",
  Pending = "pending",
  Rented = "rented"
}